import React, { Component } from 'react';
import { useTable } from 'react-table';

class TableData extends Component {
    constructor(props) {
      super(props);
      this.state = {
        data:[]
      };
    }

    

}